# 论文与实验作图准则

本仓库新增、修改的论文图按此执行。目标是：**白底、文字清楚有分量、主体突出、比较直接**。先保证正常阅读尺寸下看得清，再调整细节；不要靠缩小字体塞内容。

## 参考来源

以论文已排版的图片为视觉基准，PPT 用来核对文字属性、颜色和组织方式。

| 来源 | 重点参考 |
| --- | --- |
| [generative_fig.pptx](overleaf/powerpoint/generative_fig.pptx)，第 9 页 | 时间曲线、紫／青对照、粗体轴标题与简洁图例 |
| 同一 PPT，第 17、22 页 | 流场层级、较大的粗体标签、曲线与背景的区分 |
| 同一 PPT，第 27 页 | 模块对齐、短标签、清晰分组 |
| [论文 PDF](overleaf/Spline-Policy-A-Structured-Representation-for-Robot-Policies/maintext.pdf)，第 4、8 页 | 插入单栏后的实际可读性、留白与线条粗细 |
| [连续性图](overleaf/Spline-Policy-A-Structured-Representation-for-Robot-Policies/images/mechanism_continuity.jpg) | 红—紫—蓝曲线、少量明确标记、无坐标轴的几何示意 |
| [流场图](overleaf/Spline-Policy-A-Structured-Representation-for-Robot-Policies/images/uncertainty_mechanism_flowfield.png)、[对比图](overleaf/Spline-Policy-A-Structured-Representation-for-Robot-Policies/images/mechanism_bench_3.jpg) | 浅蓝流场、紫色执行轨迹、醒目的方法名称 |

PPT 中有素材区和未裁剪内容，不照搬整页空白。当前 Linux 没有 Myriad，LibreOffice 预览会出现字体替换和异常换行；这些不是原稿的排版准则。字体依据 PPT XML 核对，最终效果以论文成图为准。

## 字体和字号

- PPT 常用文字为 **Myriad Bold**，主要层级为 16／14／12 pt；部分早期机制图标题使用 Times New Roman Bold，公式使用 Cambria Math／Times New Roman。不能只看主题字体，也不把整篇图统一换成衬线体。
- 新实验图使用一致的无衬线文字。当前环境明确使用 **Arimo**（Arial 兼容字体），公式使用 STIX。若环境提供 Myriad，可统一替换；不要静默落到 Matplotlib 默认字体。
- 方法名称、面板标题、轴标题和简短关键标注用粗体；刻度用常规字重。避免细灰小字、全图斜体和同一张图混用多套字体。
- **按最终插入论文的宽度设置字号。** 单栏约 3.5 in，双栏约 7.2 in；不能先画 12 in 宽、8 pt 字的图，再整体缩到双栏。

下表是新实验图在最终尺寸下的默认值，不是直接复制 PPT 的绝对字号：

| 元素 | 默认字号 | 字重 |
| --- | --- | --- |
| 方法名称／主要分组 | 13–14 pt | Bold |
| 轴标题、简短关键数值 | 10–11 pt | Bold |
| 图例 | 9–10 pt | Bold |
| 刻度、次级标签 | 9 pt | Regular |

若这些字号放不下，先精简内容、调整面板数量或改双栏；不要持续缩字。必要的次级文字也不低于最终 8 pt。

## 配色及语义

同一对象在所有面板中用同一颜色；渐变用于曲线相位，单色用于方法对比。避免额外引入彩虹色或高饱和装饰色。

| 对象 | 颜色 | 来源／用法 |
| --- | --- | --- |
| 参考曲线 | `#F1454F → #E388B0 → #C0B4FF → #7296FF` | PPT 第 9、22 页的渐变色 |
| 紫色执行轨迹 | `#AD6AEA` | PPT 第 22 页；当前 M1 Numerical 在空间图和时间图中统一使用 |
| 青色对照 | `#6DD2D9` | PPT 第 9 页；当前 M1 Analytical |
| 淡紫色基线 | `#AD92E3` | PPT 第 9 页；用于其他实验时先确定语义，不与执行轨迹随意互换 |
| 流场背景 | `#8AC6ED` | 沿用仓库的浅蓝色系，低于主轨迹的视觉强度 |
| 正文／关键标记 | `#000000`／`#333333` | 清楚、稳定，不用浅灰承载必要信息 |
| 必要的分组边界 | `#D0CECE` | PPT 中的中性灰；仅在确实帮助分组时使用 |

## 布局与信息量

- 一张图围绕一个比较问题组织。方法名称只出现一次；参数、求解器设置和实验限制写在图注／正文中。
- 左右对比使用相同视角、空间范围、纵横比、时间范围和数值轴尺度。边界、标题基线、图形高度对齐。
- 主体尽量占满其面板，保留自然间距；不要在图形外再留一大圈空白，也不要为了“长条”压扁空间曲线。
- 几何示意和轨迹图不画坐标轴、刻度及网格；时间曲线保留必要的轴、单位和约 3–5 个主刻度。
- 能直接标注的关键数值就靠近对应点标注。图例只解释尚不明确的视觉编码，通常 2–3 项；不要让读者在小点、空心圈和多行图例之间反复查找。
- 优先靠对齐和留白分组。参考图中的灰框、光晕是有语义的局部手段，不默认给每个面板添加边框、阴影或色块。

## 线条、流场和标记

- 最终尺寸下，参考曲线约 2.5–3 pt，执行轨迹约 1.5–2 pt，投影箭头约 1–1.3 pt，背景流线约 0.5–0.8 pt。根据视觉检查微调，保持主次顺序。
- 参考曲线要有清楚的实线中心；光晕可少量使用，但不能成为模糊色带。实际执行轨迹保留原始折线，不作视觉平滑。
- 流场可用疏密适中的浅蓝流线和可辨认的箭头，避免铺满密集短箭头。背景要能读出方向，又不能盖住轨迹和投影箭头。
- 起点、终点和投影端点的标记必须在最终尺寸下可辨认。箭头指向真实端点，留出必要间隙；不添加与结论无关的采样点。
- 不靠改变坐标、放大单侧振幅、截断失败轨迹或增加人工波形强化差异。空间放大图必须明确对应区域，两种方法采用一致规则。

## 当前 M1 图的约定

- 保留长条布局：左半 **Numerical**，右半 **Analytical**；各自包含完整空间轨迹和原始位置时间曲线。
- 只画解析流场作为共同背景。最近点箭头使用两侧相同的真实查询状态。
- 不画 `Curve start`、曲线采样点、额外蓝色虚线；不重复写 `Trajectory` 等无区分作用的标题。
- 当前案例的局部往返、恢复时间和限制由原始数据决定。调整视觉风格不改变参数、数据、到达判据或结论。
- [绘图入口](spline_policy/run/rebuttal/plot_projection_comparison.py) 集中设置字体、颜色和布局；复用已有绘图代码，不为每次样式尝试新增脚本。

## 导出与交付检查

M7 的 GShape 对比为六方法 × 四档 σ：参考用 PPT 相位渐变，25 个样本用紫色，概率输出按用户要求改为 PPT 青色 `#6DD2D9`。Baseline 两行只画原始离散点；所有 Prob. 行保留样本簇，Prob. Flow 展示生成各流场的样条预测。

1. 优先输出嵌入字体的矢量 PDF；同时输出 SVG 和至少 300 dpi 的 PNG。SVG 若保留可编辑文字，须注明使用的字体；跨环境展示以嵌字 PDF 为准。
2. 在实际单栏／双栏宽度、100% 比例下查看，检查最小文字、箭头和曲线是否清楚。只看放大后的高分辨率 PNG 不算完成。
3. 检查标题是否对齐、文字是否裁切、单位是否一致、图例是否冗余、背景是否抢主体。
4. 修改排版后核对数据来源和绘图数组，确认没有改变实验数据或其含义。科学数据验证与样式检查分开进行。
5. 当前实验只维护一套正式文件，覆盖更新；草图和渲染中间件放临时目录，不重新堆积旧版本。
